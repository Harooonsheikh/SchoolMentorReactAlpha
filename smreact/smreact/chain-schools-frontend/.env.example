# ─── Copy to .env and adjust per environment ───

# Dev (proxied through Vite to the .NET backend):
VITE_API_BASE_URL=/api
VITE_USE_MOCK=true

# Production example (point straight at the deployed API, turn off mocks):
# VITE_API_BASE_URL=https://api.yourschoolchain.com
# VITE_USE_MOCK=false
